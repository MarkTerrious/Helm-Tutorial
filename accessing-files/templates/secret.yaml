# Helm Chart 내부에 Secret 이 포함되므로 권장되지 않는 방식
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "accessing-files.fullname" . }}
type: Opaque
data:
{{ (.Files.Glob "files/*.properties").AsSecrets | indent 2 }}